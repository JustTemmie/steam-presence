decoupled the program from steam, making it possible to just disable it entirely, meaning things such as the Epic Games Store could be supported in the future - even though it will continue to be named "steam presence"
added a debug mode to the program, it just prints extra stuff to the terminal

changed some of the log messages

fixed a bug where the program wouldn't log certain actions when changing games
